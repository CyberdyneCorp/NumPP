# Tasks — vectorized strings (numpy.char)

- [x] string->string: add, multiply, upper, lower, capitalize, title, strip/lstrip/rstrip, replace
- [x] string->int64/bool: str_len, find, count, startswith, endswith
- [x] tests: numpy oracle for numeric ops + direct equality for string results (15 cases)
- [x] clang + ASan/UBSan green
- [x] openspec validate; PR + merge + archive
