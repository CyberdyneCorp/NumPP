# Tasks — Phase 7 I/O

- [x] 1 NPY writer + reader (all dtypes, C/F order, v1.0 write / v1.0+v2.0 read); replace test-only loader
- [x] 2 NPZ savez/savez_compressed/load (ZIP of NPY; zlib when available else stored)
- [x] 3 array_str/array_repr/to_string (per-dtype format, precision, summarization, repr dtype suffix)
- [x] 4 NumPy-oracle tests; issue + regression per divergence
- [x] 5 openspec validate --strict; clang+gcc + ASan/UBSan; PR + merge + archive
