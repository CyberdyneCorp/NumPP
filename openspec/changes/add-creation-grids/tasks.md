# Tasks — creation grids

- [x] coordinate grids: meshgrid ('xy'/'ij'), indices
- [x] diagonals & triangles: diag (build/extract), diagflat, tri, tril, triu
- [x] generators: vander (increasing/decreasing), logspace, geomspace, fromfunction
- [x] NumPy-oracle tests (19 cases); clang + ASan/UBSan green
- [x] openspec validate; PR + merge + archive
